@model HolidayTracker.Views.RequestTypes.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Request Types</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<form asp-controller="RequestType" asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Find by name:
            <input type="text" name="SearchString" value="@Model.CurrentFilter" />
            <input type="submit" value="Search" class="btn btn-primary" />
            <a asp-controller="RequestType" asp-action="Index">Back to full List</a>
        </p>
    </div>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-controller="RequestType" asp-action="Index"></a>
                @Html.DisplayNameFor(model => model.RequestType[0].CompanyId)
            </th>
            <th>
                <a asp-controller="RequestType" asp-action="Index" asp-route-sortOrder="@Model.CodeSort" asp-route-currentFilter="@Model.CurrentFilter"></a>
                @Html.DisplayNameFor(model => model.RequestType[0].RequestTypeCode)
            </th>
            <th>
                <a asp-controller="RequestType" asp-action="Index" asp-route-sortOrder="@Model.NameSort" asp-route-currentFilter="@Model.CurrentFilter"></a>
                @Html.DisplayNameFor(model => model.RequestType[0].RequestTypeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RequestType[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.RequestType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RequestTypeCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RequestTypeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
